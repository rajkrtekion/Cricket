import java.util.*;
public class Main {
    public static void main(String[] args) {
            MatchController matchController=new MatchController();  //Driver for Cricket Application
            matchController.matchStart();
            matchController.matchPlay();
            matchController.matchResult();
    }
}